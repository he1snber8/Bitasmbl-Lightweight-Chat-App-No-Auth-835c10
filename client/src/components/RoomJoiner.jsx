import React,{useState}from 'react';export default function RoomJoiner({onJoin}){const[room,setRoom]=useState('');const[name,setName]=useState('');return(<form onSubmit={e=>{e.preventDefault();onJoin({room,name});}}><input value={room}onChange={e=>setRoom(e.target.value)}placeholder="Room"/><input value={name}onChange={e=>setName(e.target.value)}placeholder="Name"/><button>Join</button></form>);}